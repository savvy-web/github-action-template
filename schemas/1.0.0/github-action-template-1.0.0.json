{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"$id": "https://raw.githubusercontent.com/savvy-web/github-action-template/main/schemas/1.0.0/github-action-template-1.0.0.json",
	"type": "object",
	"properties": {
		"$schema": {
			"type": "string",
			"description": "URL of the JSON Schema document that validates this payload."
		},
		"greeting": {
			"type": "string",
			"description": "The rendered greeting. Empty string when the run failed before the greeting was composed."
		},
		"summaryWritten": {
			"type": "boolean",
			"description": "Whether the job summary panel was appended. False when the write-summary input disabled it, the run was a rehearsal, or the write degraded to a warning."
		},
		"dryRun": {
			"type": "boolean",
			"description": "Whether the run was a rehearsal, performing no mutation."
		}
	},
	"required": ["$schema", "greeting", "summaryWritten", "dryRun"],
	"additionalProperties": false,
	"title": "Action run result",
	"description": "The structured `result` output of savvy-web/github-action-template: what the run greeted, whether it wrote a job summary, and whether it was a rehearsal.\nhttps://github.com/savvy-web/github-action-template/blob/main/docs/04-output-schema.md"
}
